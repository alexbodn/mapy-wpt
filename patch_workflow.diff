--- .github/workflows/android.yml
+++ .github/workflows/android.yml
@@ -43,15 +43,15 @@
     - name: Upload APK Artifact
       uses: actions/upload-artifact@v4
       with:
-        name: mapy-wpt-saver-release
+        name: Mapy-to-GPX-Saver-release
         path: app/build/outputs/apk/release/app-release.apk

     - name: Rename APK for Release
-      run: mv app/build/outputs/apk/release/app-release.apk MapyWptSaver-release-${{ github.run_number }}.apk
+      run: mv app/build/outputs/apk/release/app-release.apk Mapy-to-GPX-Saver-release-${{ github.run_number }}.apk

     - name: Create Release
       if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
       uses: softprops/action-gh-release@v1
       with:
-        files: MapyWptSaver-release-${{ github.run_number }}.apk
+        files: Mapy-to-GPX-Saver-release-${{ github.run_number }}.apk
         tag_name: v1.0.${{ github.run_number }}
